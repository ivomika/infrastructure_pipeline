name: Dependency update gates

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/dependency-update.yml"
      - ".github/workflows/reproducibility.yml"
      - "renovate.json"
      - "jenkins/toolchain.lock"
      - "docmost/toolchain.lock"
      - "docmost/**"
      - "plane/toolchain.lock"
      - "plane/**"
      - "jenkins/plugins/**"
      - "scripts/refresh-locks.sh"
      - "scripts/security-scan.sh"

permissions:
  contents: read

concurrency:
  group: dependency-update-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-locks:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Check out the proposed lock change
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Install Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3
        with:
          install: true

      - name: Verify registries, artifacts, and official checksums
        run: scripts/verify-locks.sh

  security-and-smoke:
    needs: validate-locks
    runs-on:
      - self-hosted
      - linux
      - x64
      - jenkins-infra
    timeout-minutes: 180
    concurrency:
      group: dependency-update-smoke
      cancel-in-progress: false
    env:
      COMPOSE_PROJECT_NAME: dependency-update-${{ github.run_id }}-${{ github.run_attempt }}
      JENKINS_HOME_VOLUME: dependency-update-${{ github.run_id }}-${{ github.run_attempt }}-jenkins-home
      JENKINS_HOST_ADDRESS: 127.0.0.1
      JENKINS_HTTP_PORT: 18080
      JENKINS_ADMIN_ID: dependency-ci
      JENKINS_ADMIN_PASSWORD: dependency-ci-${{ github.run_id }}-${{ github.run_attempt }}
      DOCMOST_HOST_ADDRESS: 127.0.0.1
      DOCMOST_HTTP_PORT: 13000
      DOCMOST_APP_URL: http://127.0.0.1:13000
      DOCMOST_DB_PASSWORD: dependency-docmost-db-${{ github.run_id }}-${{ github.run_attempt }}
      DOCMOST_APP_SECRET: dependency-docmost-app-secret-${{ github.run_id }}-${{ github.run_attempt }}
      DOCMOST_REDIS_PASSWORD: dependency-docmost-redis-${{ github.run_id }}-${{ github.run_attempt }}
      DOCMOST_OWNER_PASSWORD: dependency-docmost-owner-${{ github.run_id }}-${{ github.run_attempt }}
      PLANE_HOST_ADDRESS: 127.0.0.1
      PLANE_HTTP_PORT: 19010
      PLANE_APP_DOMAIN: 127.0.0.1
      PLANE_WEB_URL: http://127.0.0.1:19010
      PLANE_CORS_ALLOWED_ORIGINS: http://127.0.0.1:19010
      PLANE_DB_PASSWORD: dependency-plane-db-${{ github.run_id }}-${{ github.run_attempt }}
      PLANE_REDIS_PASSWORD: dependency-plane-redis-${{ github.run_id }}-${{ github.run_attempt }}
      PLANE_RABBITMQ_PASSWORD: dependency-plane-rabbit-${{ github.run_id }}-${{ github.run_attempt }}
      PLANE_MINIO_ROOT_PASSWORD: dependency-plane-minio-${{ github.run_id }}-${{ github.run_attempt }}
      PLANE_SECRET_KEY: dependency-plane-secret-${{ github.run_id }}-${{ github.run_attempt }}
      PLANE_LIVE_SERVER_SECRET_KEY: dependency-plane-live-${{ github.run_id }}-${{ github.run_attempt }}
      SHARED_LIBRARY_NAME: dependency-ci
      SHARED_LIBRARY_REPOSITORY_URL: ssh://git@invalid/dependency-ci.git
      SHARED_LIBRARY_DEFAULT_BRANCH: main
      SHARED_LIBRARY_CREDENTIALS_ID: dependency-ci
    steps:
      - name: Check out the proposed lock change
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Install Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3
        with:
          install: true

      - name: Prepare isolated runtime inputs
        run: |
          install -m 600 .env.example .env
          rm -f \
            jenkins/secrets/known_hosts \
            jenkins/secrets/shared-library.key \
            jenkins/secrets/shared-library.key.pub
          ssh-keygen -q -t ed25519 -N "" -f jenkins/secrets/shared-library.key
          awk '{ print "invalid " $1 " " $2 }' \
            jenkins/secrets/shared-library.key.pub \
            >jenkins/secrets/known_hosts
          chmod 600 jenkins/secrets/shared-library.key

      - name: Build all locked images
        run: scripts/build-images.sh

      - name: Scan infrastructure configuration
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: config
          scan-ref: .
          # renovate: datasource=github-releases depName=aquasecurity/trivy versioning=semver
          version: v0.70.0
          severity: CRITICAL
          ignore-unfixed: true
          exit-code: 1

      - name: Scan controller, agents, and runtime images
        run: scripts/security-scan.sh

      - name: Start the isolated stack
        run: |
          export SOURCE_DATE_EPOCH
          SOURCE_DATE_EPOCH="$(git log -1 --format=%ct)"
          docker compose up --detach --wait --wait-timeout 900

      - name: Provision every ephemeral agent
        run: scripts/smoke-test.sh

      - name: Show logs after a failure
        if: failure()
        run: |
          export SOURCE_DATE_EPOCH
          SOURCE_DATE_EPOCH="$(git log -1 --format=%ct)"
          docker compose logs --no-color --tail 200

      - name: Remove the isolated test stack
        if: always()
        run: |
          cleanup_status=0
          export SOURCE_DATE_EPOCH
          SOURCE_DATE_EPOCH="$(git log -1 --format=%ct)"
          docker compose down --volumes --remove-orphans || cleanup_status=$?
          rm -f \
            .env \
            jenkins/secrets/known_hosts \
            jenkins/secrets/shared-library.key \
            jenkins/secrets/shared-library.key.pub
          exit "$cleanup_status"
