# DevOps infrastructure

Локальная инфраструктура для CI/CD, управления задачами, документации,
артефактов и мониторинга. Все приложения объединяются корневым
`compose.yaml` через Compose `include`.

## Запуск

```sh
make up
```

При первом запуске `.env` создаётся из `.env.example`.
