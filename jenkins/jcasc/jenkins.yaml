jenkins:
  systemMessage: "Minimal Jenkins in Docker managed by JCasC"
  numExecutors: 1
  mode: NORMAL
  securityRealm:
    local:
      allowsSignup: true
      users:
        - id: "${JENKINS_ADMIN_ID}"
          password: "${JENKINS_ADMIN_PASSWORD}"
  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false

credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: "${SHARED_LIBRARY_CREDENTIALS_ID}"
              username: "${SHARED_LIBRARY_GIT_USERNAME}"
              password: "${SHARED_LIBRARY_GIT_PASSWORD}"
              description: "Git credentials for Jenkins shared library"

unclassified:
  location:
    url: "${JENKINS_URL}"
  globalLibraries:
    libraries:
      - name: "${SHARED_LIBRARY_NAME}"
        defaultVersion: "${SHARED_LIBRARY_DEFAULT_BRANCH}"
        implicit: false
        allowVersionOverride: true
        includeInChangesets: false
        retriever:
          modernSCM:
            scm:
              git:
                remote: "${SHARED_LIBRARY_REPOSITORY_URL}"
                credentialsId: "${SHARED_LIBRARY_CREDENTIALS_ID}"
